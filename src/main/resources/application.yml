server:
  tomcat:
    max-swallow-size : 100MB
  port: 8080
  servlet:
      context-path: /asset
spring:
  application:
    name: AssetManagement
  datasource:
    url: jdbc:mariadb://localhost:3306/asset
    username: root
    password: 123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql